# LLM / Ollama Configuration

**See [Rationale and LLM](rationale-and-llm.md)** for how rationales work, the LLM chain (OpenAI → Ollama → rule-based), model defaults (gpt-4o-mini, llama3.2), and full Ollama env vars (`OLLAMA_HOST`, `OLLAMA_MODEL`, `OLLAMA_TIMEOUT`).
