# NBA Stat Spot API container — lightweight maintenance only.
# Data ingest/build/settle runs in the pipeline container (see pipeline_crontab).

# Cleanup expired cache entries daily at 3:00 AM UTC
0 3 * * * curl -sf -X POST "http://localhost:8007/api/v1/admin/cache/cleanup" || echo "Failed: cache cleanup"
